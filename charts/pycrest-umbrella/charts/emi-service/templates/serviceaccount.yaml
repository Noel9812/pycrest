apiVersion: v1
kind: ServiceAccount
metadata:
  name: emi-service-sa
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "common.labels" . | nindent 4 }}
automountServiceAccountToken: false