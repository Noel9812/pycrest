apiVersion: v2
name: loan-service
description: >
  PayCrest Loan Service — core loan lifecycle: applications, KYC,
  document uploads, sanction letters, eligibility, customer portal,
  foreclosure, NOC generation, and EMI schedule creation.
type: application
version: 1.0.0
appVersion: "1.0.0"
dependencies:
  - name: common
    version: "1.0.0"
    repository: "file://../common"