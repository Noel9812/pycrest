apiVersion: v2
name: api-gateway
description: >
  PayCrest API Gateway — Node.js/Express reverse proxy that routes all
  /api/* requests to the appropriate backend microservice. Handles
  authentication forwarding, CORS, and request logging.
type: application
version: 1.0.0
appVersion: "1.0.0"
dependencies:
  - name: common
    version: "1.0.0"
    repository: "file://../common"