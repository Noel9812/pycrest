apiVersion: v2
name: manager-service
description: >
  PayCrest Manager Service — manager dashboard, loan queue assignment,
  verification team management, sanction letter verification, and
  manager approvals.
type: application
version: 1.0.0
appVersion: "1.0.0"
dependencies:
  - name: common
    version: "1.0.0"
    repository: "file://../common"