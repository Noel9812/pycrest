apiVersion: v2
name: frontend
description: >
  PayCrest Frontend — React/Vite SPA served by Nginx. Proxies /api/*
  requests to the api-gateway service internally within the cluster.
type: application
version: 1.0.0
appVersion: "1.0.0"
dependencies:
  - name: common
    version: "1.0.0"
    repository: "file://../common"